<Window x:Class="CodeBridge.Views.CyberCloseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="关闭程序"
        Width="480" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        FontFamily="Microsoft YaHei UI"
        KeyDown="Window_KeyDown"
        Opacity="0">

    <Window.Resources>
        <!-- 颜色定义 -->
        <Color x:Key="NeonBlue">#00f3ff</Color>
        <Color x:Key="NeonRed">#ff0055</Color>
        <SolidColorBrush x:Key="NeonBlueBrush" Color="{StaticResource NeonBlue}"/>
        <SolidColorBrush x:Key="NeonRedBrush" Color="{StaticResource NeonRed}"/>
        <SolidColorBrush x:Key="CardBg" Color="#14191F" Opacity="0.9"/>

        <!-- 选项卡片样式 - 推荐 -->
        <Style x:Key="RecommendedCardStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="CardBorder"
                                    Background="Transparent"
                                    BorderBrush="#33FFFFFF"
                                    BorderThickness="1"
                                    Padding="20">
                            </Border>
                            <!-- 顶部动画线条 -->
                            <Border x:Name="TopLine"
                                    Height="2"
                                    Width="0"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left"
                                    Background="{StaticResource NeonBlueBrush}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource NeonBlue}" BlurRadius="8" ShadowDepth="0"/>
                                </Border.Effect>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#6600f3ff"/>
                                <Setter TargetName="CardBorder" Property="Background" Value="#1A00f3ff"/>
                                <Setter TargetName="CardBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource NeonBlue}" BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="TopLine"
                                                             Storyboard.TargetProperty="Width"
                                                             To="180" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="TopLine"
                                                             Storyboard.TargetProperty="Width"
                                                             To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 选项卡片样式 - 危险 -->
        <Style x:Key="DangerCardStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="CardBorder"
                                    Background="Transparent"
                                    BorderBrush="#33FFFFFF"
                                    BorderThickness="1"
                                    Padding="20">
                            </Border>
                            <!-- 顶部动画线条 -->
                            <Border x:Name="TopLine"
                                    Height="2"
                                    Width="0"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left"
                                    Background="{StaticResource NeonRedBrush}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource NeonRed}" BlurRadius="8" ShadowDepth="0"/>
                                </Border.Effect>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#66ff0055"/>
                                <Setter TargetName="CardBorder" Property="Background" Value="#1Aff0055"/>
                                <Setter TargetName="CardBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource NeonRed}" BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="TopLine"
                                                             Storyboard.TargetProperty="Width"
                                                             To="180" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="TopLine"
                                                             Storyboard.TargetProperty="Width"
                                                             To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 返回按钮样式 -->
        <Style x:Key="ReturnButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                BorderBrush="#55FFFFFF"
                                BorderThickness="1"
                                Padding="30,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 主容器 -->
    <Grid>
        <!-- 背景 -->
        <Border Background="#0a0b10" CornerRadius="0" Margin="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
            </Border.Effect>
        </Border>

        <!-- 网格背景 -->
        <Border Margin="8" ClipToBounds="True">
            <Border.Background>
                <DrawingBrush TileMode="Tile" Viewport="0,0,40,40" ViewportUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <LineGeometry StartPoint="0,0" EndPoint="40,0"/>
                                    <LineGeometry StartPoint="0,0" EndPoint="0,40"/>
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                            <GeometryDrawing.Pen>
                                <Pen Brush="#1500f3ff" Thickness="1"/>
                            </GeometryDrawing.Pen>
                        </GeometryDrawing>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Border.Background>
        </Border>

        <!-- 外框切角效果 -->
        <Border Margin="8" BorderThickness="1" BorderBrush="#3300f3ff">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource NeonBlue}" BlurRadius="20" ShadowDepth="0" Opacity="0.15"/>
            </Border.Effect>
        </Border>

        <!-- 顶部装饰线 -->
        <Border Height="1" VerticalAlignment="Top" Margin="60,8,60,0" Background="{StaticResource NeonBlueBrush}" Opacity="0.5">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource NeonBlue}" BlurRadius="10" ShadowDepth="0"/>
            </Border.Effect>
        </Border>

        <!-- 主要内容 -->
        <Grid Margin="25,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题区域 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,5,0,15">
                <TextBlock Text="SYSTEM TERMINATION"
                           Foreground="{StaticResource NeonBlueBrush}"
                           FontSize="10"
                           HorizontalAlignment="Center"
                           Opacity="0.7"/>
                <TextBlock Text="关闭程序"
                           Foreground="White"
                           FontSize="22"
                           FontWeight="Bold"
                           HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="{StaticResource NeonBlue}" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <!-- 选项卡片区域 -->
            <Grid Grid.Row="1" Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 最小化到托盘 -->
                <Button Grid.Column="0" Style="{StaticResource RecommendedCardStyle}" Click="MinimizeToTray_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 推荐标签 -->
                        <Border Background="{StaticResource NeonBlueBrush}"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="0,-12,-12,0"
                                Padding="6,2">
                            <TextBlock Text="RECOMMENDED" FontSize="8" FontWeight="Bold" Foreground="#0a0b10"/>
                        </Border>

                        <!-- 图标 -->
                        <TextBlock Grid.Row="0" Text="📥" FontSize="28" HorizontalAlignment="Center" Margin="0,5,0,8"/>

                        <!-- 标题 -->
                        <TextBlock Grid.Row="1" Text="最小化到托盘"
                                   FontSize="15" FontWeight="Bold" Foreground="White"
                                   HorizontalAlignment="Center" Margin="0,0,0,6"/>

                        <!-- 描述 -->
                        <TextBlock Grid.Row="2"
                                   Text="程序将在后台继续运行，您仍可接收任务通知与实时同步。"
                                   FontSize="11" Foreground="#a0a0a0"
                                   TextWrapping="Wrap" TextAlignment="Center"
                                   LineHeight="16"/>
                    </Grid>
                </Button>

                <!-- 完全退出 -->
                <Button Grid.Column="2" Style="{StaticResource DangerCardStyle}" Click="Exit_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 图标 -->
                        <TextBlock Grid.Row="0" Text="✖" FontSize="28" Foreground="{StaticResource NeonRedBrush}" HorizontalAlignment="Center" Margin="0,5,0,8"/>

                        <!-- 标题 -->
                        <TextBlock Grid.Row="1" Text="完全退出程序"
                                   FontSize="15" FontWeight="Bold" Foreground="White"
                                   HorizontalAlignment="Center" Margin="0,0,0,6"/>

                        <!-- 描述 -->
                        <TextBlock Grid.Row="2" TextWrapping="Wrap" TextAlignment="Center" FontSize="11" Foreground="#a0a0a0" LineHeight="16">
                            <Run Text="终止所有进程。您将"/>
                            <Run Text="无法" Foreground="{StaticResource NeonRedBrush}"/>
                            <Run Text="收到任何实时通知与数据更新。"/>
                        </TextBlock>
                    </Grid>
                </Button>
            </Grid>

            <!-- 底部返回按钮 -->
            <Button Grid.Row="2"
                    Content="返 回"
                    Style="{StaticResource ReturnButtonStyle}"
                    Click="Cancel_Click"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,5"/>
        </Grid>
    </Grid>
</Window>
